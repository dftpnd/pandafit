[package]
name = "pandafit-media"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2.189"
pandafit-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
